# Lab 3 — AI Use and Reflection  (fill this in)

**LLM/agent used:** Antigravity (Gemini 3.7 Flash / Gemini 3.8 Flash)

## Selected key prompts (6–10)
| # | Prompt (summarised) | What I did with the result | My Reflection |
|---|---------------------|----------------------------|---------------|
| 1 | Read provided Lab 3 handout PDF and draft required specification documents in `docs/lab-03/` (`specification.md`, `ui-spec.md`, `api-spec.md`, `tests.md`). Do not touch other directories, source code, or `reviewer.md`/`ai-use.md`. | Verified the generated specifications, Zen Green UI tokens, role-based API matrix, and test traceability against the labsheet. | Setting strict file boundaries prevents the agent from touching existing code. |
| 2 | Fed partner's review feedback on email domain regression (`@kmutt.ac.th` vs `@toktickit.com`), staff attachment permissions (Option B), and Enum migration (`ALTER TYPE ... RENAME VALUE`). Required an impact analysis and plan before implementing. | Reviewed the impact analysis, approved the dual-domain seed strategy (preserving Lab 2 regression while introducing new staff accounts), and updated specs. | Forcing the agent to analyze architectural tradeoffs before writing specs prevented test breakages and ensured we met all course requirements. |
| 3 | Investigated compiled `.js` files appearing alongside `.tsx` in `client/src/`. Instructed the agent to diagnose root cause configurations without modifying code yet, identify what generated them, and propose a clean fix. | Approved the plan to adjust `tsconfig.json` (`noEmit: true`), cleanly removed duplicate `.js` files, and verified Vite dev server integrity. | Telling the AI "do not modify code yet, trace the configuration first" prevented accidental source deletions. |
| 4 | Pointed out UI polish issue (lack of spacing between loading spinner and "Posting..." label in post comment button) and verified whether IT Staff should access requester ticket screens. | Verified the spacing fix and enforced proper RBAC where IT Staff are routed to their dedicated operational view (`/queue`). | Combining visual UI feedback kept user experience and authorization tightly aligned. |
| 5 | In `/queue`, clicking a ticket navigated to `/queue/:id` but rendered "My Tickets". Asked the agent to diagnose if this was intended scaffolding for the current issue before writing any code. | Confirmed this was expected sprint staging (Staff Detail was assigned to Issue #30) and avoided premature implementations across issue boundaries. | Asking the agent to clarify scope prevented unnecessary code outside the active issue. |
| 6 | Identified redundant status/priority badges next to interactive dropdowns, unnecessary `(Current)` label suffixes, and duplicate `Claim Ticket (Michael Brown)` options in Ticket Owner selector. Instructed agent to clean them up. | Verified the removal of redundant visual clutter and dropdown duplicates on the IT Staff detail screen. | Manual inspection of UI caught component redundancies that automated tests couldn't detect. |
| 7 | Shared peer reviewer comment: when changing status to `RESOLVED`, errors displayed inside the modal, but transitions to `CLOSED` or `REOPENED` popped up on the main page banner. Asked agent to verify and unify error display inside modals. | Verified the bug in `StaffTicketDetailScreen.tsx` and applied the fix to ensure consistent modal error positioning across all status transitions. | Passing my reviewer's feedback directly into the prompt with "verify first, don't modify yet" helped the agent quickly pinpoint the UX inconsistency and implement a clean fix. |
| 8 | Handled peer review feedback on duplicate eye password toggle icons, missing sign-out option when locked on `/change-password`, and redundant active radio vs button in the edit drawer. | Fixed the input-group eye icon alignment, added sign-out escape hatch on the mandatory password change screen, and streamlined the user status drawer. | Reviewing peer suggestions allowed us to improve system ergonomics without violating safety rule. |
| 9 | Agent pushed to `lab3-staging` without authorization. Commanded an immediate revert on `lab3-staging` and strictly constrained subsequent work to local commits on `feature/lab3-e2e-and-release`. | Successfully reverted `lab3-staging` back to clean merge commit `f99559e` and enforced local-only Git operations. | Strict branching discipline is vital when working with autonomous agents; Almost got cooked if I wasn't actively checking it at the moment fr. |
| 10 | Reported that `/admin/users` failed to adapt to mobile screens. Required an implementation plan before writing code, then implemented stacked mobile cards with $\ge 44\text{px}$ touch targets and verified with Playwright. | Reviewed the plan, implemented the mobile responsive cards for viewports $< 768\text{px}$, and verified zero horizontal overflow across Desktop, Tablet, and Mobile. | Mobile viewports require explicit structural adaptations. Requesting a plan first guaranteed accessibility and touch target compliance. |

## Reflection
Two or three sentences: what made your prompts better, and one place you had to
correct or reject what the agent produced.

Establishing clear boundaries with `AGENTS.md`, enforcing diagnose and plan first before touching code, and feeding peer review feedback directly into prompts significantly elevated implementation quality and eliminated scope creep. However, the agent occasionally rushed into git operations without explicit permission, which required immediate rejection, git reverts from me.
